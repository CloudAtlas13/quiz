//Definimos un modelo de base de datos para los favoritos
//tal que se puedan almacenar las asignaciones de quizes
//a users

module.exports = function(sequelize, DataTypes){
  return sequelize.define('Favourite', {
    
  });
};
