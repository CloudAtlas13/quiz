extend ../layout

block content
  p Editar información de la cuenta

  form(method="post", action="/user/#{user.id}?_method=put")
    include _form.jade

  form(method="post", action="/user/#{user.id}?_method=delete")
    button(type="submit", unClick="return confirm('Borrar cuenta: #{user.username}');") Eliminar cuenta
